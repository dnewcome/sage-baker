.venv/
__pycache__/
*.pyc
.sagemaker/
.sm-scratch/
.sb-test/
output/
model/
model_*/
materialized/

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Generated data — prepare-* scripts regenerate these
data/*.csv
data/*.parquet
data/lineage.json
mlflow.db
mlartifacts/
mlruns/

# Feast: source files are tracked, generated state is not.
feature_repo/data/*.parquet
feature_repo/data/*.db

# Private plugins — company-specific; never in the public repo
src/plugins/private/
Makefile.private

# Claude Code: ship the project skills, ignore per-user settings
.claude/*
!.claude/skills/

# Credentials / secrets — never commit
*-key.json
*-credentials.json
service-account*.json
gcp-*.json
gen-lang-client-*.json
client_secret_*.json
secrets/
.secrets/
.env
.env.local
